# Installation Guide

1. Clone the repository:
   ```sh
   git clone <repository_url>
   cd <repository>
   ```

2. Install dependencies:
   ```sh
   pip install -r requirements.txt
   ```

3. Run the script:
   ```sh
   python main.py
   
